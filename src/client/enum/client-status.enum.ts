export enum ClientStatusEnum {
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  EXPIRE = 'expire',
  SENT = 'sent',
  DECLINED = 'declined',
  pending = 'pending'
}

export enum ClientStatus {
  Active = "Active",
  InActive = "InActive"
}